
namespace some.name.collections;

use blah.interop.clr.SomeClass;
use some.StaticClass<String>.Nested;

public abstract class MutableList<T> {
  let backingArray: Array<T> = new Array<T>();

  protected override def aVeryFancyMethod<E extends bleh> (test: E) {
    test.sayHello()
    |> string.doSomethingFancy
    |> blah.hello.world;
  }

  private def thingOfThing<a implements IDisposable> (): () {
    compo >> sition
  }
}
