
public class OverloadingExample {
  public def myMethod (one) {
    while (one) {
      42 * 2
    }
  }

  public def myMethod (one, two) {
    9 + ((10 * 21) / 4)
  }

  class AbstractOverloads {
    abstract def thing (one: int) : ();
    abstract def thing (one: int, two: int) : int;
  }
}
