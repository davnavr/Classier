namespace Classier.NET.Compiler.SemAnalysis

type ClassAnalysis = unit

module ClassAnalyzer =
    let analyze gtable syntax cdef =
        // TODO: Check for members, valid superclass, and valid interface impls.
        ()
