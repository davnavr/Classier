
public class MyException1 extends System.Exception {
    let myField = throw "test" /* please work */;

    public doSomething () {
        throw new MyException1();
    }

    public somethingElse (one, two, three, four, five) {
        try {
            doSomething();
        }
        catch {
            e: System.ArgumentException => System.Console.WriteLine(e);
            _ => printfn("this is the default handler");
        }
        finally {
            // take out the trash
            one.Dispose();
        }
    }

    public two (three) {
      try {
        somethingElse (three, three, three, this, three);
      }
      finally {
        printfn ("line 1\u000Aline 2");
        three.close ();
      }
    }
}
