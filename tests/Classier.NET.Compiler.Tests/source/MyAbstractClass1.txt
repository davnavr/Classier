
namespace this.is.my.space;

use java.lang;
use java.util;

internal abstract class MyAbstractClass1 {
  let tester: java.lang.String = someUndefinedGlobal;

  let complexDecisions =
        if (1) {
            testing + 1
        }
        else if (2) {
            Console.WriteLine(12362367);
            help
        }
        else if (3) {
            oh
        }
        else {
            no
        };

  public abstract noBody (iUsedToKnow);

  private inline doesSomething () {
    let myCurriedResult = this.noBody (45.6) (blah);

    if (iExist) {
        java.lang.System.out.println(this);
    }

    tester.doThingsWith(this);
  }
}
