
use System._;
use System.IO.FileInfo;

// This is a comment
public class MyClass1 {
    let myField: int = 42;

    // Optional type annotation for return type
    public TheTest (myParam: FileInfo)
    {
        while (thisWorks) {
            42 * 2;
        }
        ;
        84;
    }

    public ExplicitTest (p1: int) (p2: int): int
    {
        /* A fancier comment */
        var someProceduralCode = p1;
        someProceduralCode <- p2;
        return (myField) / 42;
    }

    public implicitTest (p1, p2)
    {
        let myLocal = (1 + 2) / 3;
        Console.WriteLine();
        1 + 2 // An implicit return value
    }

    public yes (no) (um) => no.add/* caramel sauce */(4722);
}
